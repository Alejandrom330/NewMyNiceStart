<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uem.dam.mynicestart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_tent_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_tent_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyNiceStart">
        <activity android:name=".MainActivity2"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".SplashActivity" android:theme="@style/Theme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SigninActivity" />
    </application>

</manifest>